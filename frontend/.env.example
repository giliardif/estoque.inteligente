# Copie para .env.local (desenvolvimento) ou configure direto no painel da Vercel
# (staging/produção). NUNCA commitar valores reais além deste exemplo.

NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
